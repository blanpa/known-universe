# Raw catalogue dumps — hundreds of MB, all regenerable
# (see pipeline/README.md for the fetch commands).
pipeline/raw/

# Build output
node_modules/
dist/
dist-artifact/
__pycache__/
*.pyc

# Written by the live-data job / updater, not by hand
site/data/live-extra.json
